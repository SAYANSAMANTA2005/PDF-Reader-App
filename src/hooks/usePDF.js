import { usePDF } from '../context/PDFContext';

export default usePDF;
// Re-exporting for convenience if needed, though usually imports directly from context file.
// Ideally usage in components is: import { usePDF } from '../context/PDFContext';
